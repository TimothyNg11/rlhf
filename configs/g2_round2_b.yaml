inherit: g2_round2.yaml
run_name: g2_round2_b
# Confirmation arm (run only if g2_round2 shows incremental lenient delta CI>0
# and >= +1.0pp): same parent + map, different RNG (launched with --seed 1).
# AMENDED 2026-07-25: originally an independent-lineage repeat (seed-1 parent),
# downgraded to same-parent/different-RNG because seed-1's checkpoint was lost
# with the pod volume. Guards run-to-run luck, not parent-checkpoint luck.
